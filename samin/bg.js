const bady = document.querySelector("body");

const IMG_NUMBER = 3;


function paintImage(imgNumber){
    const image = new Image();
    imgMumber.src = `images/${imgNumber + 1}.jpg`;
    image.classList.add();
    body.prepend(image);
}

function genRandom(){
    const random = Math.floor(Math.random() * IMG_NUMBER);
    return Number;
}

function init(){
    const randomNumber;
}